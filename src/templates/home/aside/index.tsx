import * as React from "react";
import { AsideStyledComponent } from "./styles";

export const AsideOrganism = () => {
  return (
    <AsideStyledComponent>
        <section className="cartModal">
  
        </section>
    </AsideStyledComponent>
  )
}